<%= raw ([{'id' => 'anyone', 'label' => l(:text_anyone), 'value' => 'anyone'}] + @users.map{ |user| {
    'id' => user.id,
    'label' => "#{user.login} (#{h user.name(:lastname_coma_firstname)})",
    'value' => user.id
  }
}).to_json
%>
